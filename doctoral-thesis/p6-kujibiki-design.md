# P6 くじ引きロボット実験 設計草案

> 博论 banner ④（統合＋帰結のある現場検証）的落地设计。
> **设计原则：不新造范式——直接继承 P1-2 酒精（消毒液）实验的已验证范式，
> 只插入一个新变量（注意通道）。**
> 主线文档：[mainline-notes.md](mainline-notes.md)。最后更新：2026-07-15

## 1. 一句话

> 公共空间通道旁的くじ引きロボット：路人自由来去，机器人主动招呼、在多人之间分配注意、
> 劝诱抽くじ；**任务成果＝くじ配布率（自动记录）**。
> 实验范式逐项复刻 P1-2 的消毒液推广实验，唯一新变量＝**注意目标的选择时机**
> （C1 反応的 vs C2 先読み/PSSP）。

## 2. P1-2 范式对应表（逐项继承）

| 要素 | P1-2 酒精实验 | P6 くじ引き | 备注 |
|---|---|---|---|
| 场所 | 商场通道（LaLaport） | 公共空间通道 | 同构：通行人流经、自由来去 |
| 任务 | 劝诱使用消毒液 | 劝诱抽纸质くじ | 同构：短交互・低门槛・善意任务 |
| 成果事件 | 消毒液被使用（喷头动作＝自动检出） | くじ被取走（**出签口传感器**＝自动检出） | **仪器化几乎相同**：dispenser 触发即成功 |
| 主指标 | 使用数/使用率（对比人类操作者） | **配布率**＝配布成功 ÷ 接近者数 | 同货币：任务成果，非印象 |
| 交互对象 | 自由通行的访客（不招募、不报酬） | 同 | 中村案的"気軽な設定"＝P1-2 本来的结构 |
| 行动库 | 言语 HELLO/SANITIZER/THANKS/AWAY ＋ 非言语 TRACK/INITIALIZE/WAIT | 言语 HELLO/**KUJI**/THANKS/AWAY ＋ 非言语 TRACK/INITIALIZE/WAIT | 只把 SANITIZER 换成 KUJI（劝诱语），七动作结构原样 |
| 状态输入 | 摄像头的访客状态 | 摄像头访客状态 ＋ **声学地图（新增）** | 新增的感知模态＝Field A 的入口 |
| 分析轴 | 不同客流量下的性能稳定性 | **混乱度分层**（1 人 vs 多人场面） | 直接对应 P1 的 traffic-level 分析 |
| 平台 | Geminoid-F android | Boxie（推荐，见 §6） | 差异点，如实交代 |

**博论逻辑（一句话）**：P1-2 在此范式下证明了"**行動の能動性**"产生任务成果；
P6 在**同一范式**上、把行动层钉死，检验"**注意の先読み性**"是否产生任务成果。
范式已被验证 ＋ 单一新变量 → 差异归因干净，且统合（Field A 插进 Field B 的范式）是字面意义的。

## 3. 三层控制架构（什么变、什么不变）

```
┌─ 注意层（WHO を見るか・いつ向くか）───────── ★実験変数★
│    C1: DoA（当前声源/接近者に反応）
│    C2: PSSP（予測された近未来の声源へ先回り）
│    → TRACK 动作的目标由此层供给；下游运动控制器全条件共通
│      （P5 の shared target-selection framework を現場に移植）
│
├─ 行动层（WHAT・WHEN）────────────────────── 全条件共通（钉死）
│    P1-2 七动作库的状态机（§4）；
│    状态机的输入 = 「注意层当前指向的对象」的状态
│    → 注意指对了人，HELLO/KUJI 才来得及 —— 帰結由此产生
│
└─ 对话层（想定外的应答）──────────────────── 全条件共通
     固定 teleop（本人）：只应答路人的自由提问；
     不控制头部、不触发动作（避免污染实验变量）
```

- HELLO/KUJI/THANKS/AWAY 用**预录/合成的固定语句**——P1-2 本来就是预定义言语动作，
  这里照搬；teleop 只兜底想定外对话（次数记录，作协变量）。
- 行动层可二选一（见 §9 TODO）：**B 案（推荐）固定状态机**——规则简单透明、全条件严格一致；
  A 案（更像 P1）预备实验时由 teleop 示范、IRL 学一个轻量行动策略——学术上更美，
  但 9 月工期风险大。**先按 B 案设计，A 案作为如果预备实验顺利的升级项。**

## 4. 行动层状态机（全条件共通）

| 状态 | 迁移条件（自动检出） | 触发动作 |
|---|---|---|
| WAIT | 无人在半径 r 内 | 待机姿态；注意层继续扫描 |
| APPROACH | 注意对象进入 r 且朝向机器人 | **HELLO**（呼びかけ）＋ TRACK |
| ENGAGED | 对象停留 > t 秒 | **KUJI**（"くじ引きいかがですか"）＋指向出签口 |
| SUCCESS | 出签口传感器触发（签被取走） | **THANKS** |
| LEAVE | 对象离开 r | **AWAY**（见送り）→ INITIALIZE |

- 多人场面：状态机跟随**注意层当前指向的对象**运行——这正是注意变得有帰結的机制：
  **看谁 → 招呼谁 → 谁停下 → 谁抽签**。C2 若能"先一步"指向将要接近/将要说话的人，
  HELLO 就来得及；C1 只能事后追赶。
- 参数 r（接近半径）、t（停留阈值）由预备实验校准，全条件共用。

## 5. 条件与日程设计

| 条件 | 注意目标的决定 | 角色 |
|---|---|---|
| **C1 反応的（DoA）** | 当前声源/当前接近者 | 基线（事后追赶） |
| **C2 先読み（PSSP）** | 预测的近未来声源（+1s 等，用 Item 3 新模型） | 主条件（先于事件转向） |
| （C3 Random） | 与观测无关切换 | 可选锚点，按人流量取舍 |

- **时间块 between 设计**：每小时轮换条件、镜像 counterbalance
  （日1：C1→C2→C2→C1；日2：C2→C1→C1→C2），周末 2 日想定。
- 分析：块级配布率，混合模型（时间带/日 随机效应）；**按混乱度（1 人 vs 多人）分层**
  ——先読み价值假说预测差异集中在多人场面（≒P1 的客流分析轴）。

## 6. 平台与配布机构（Boxie 案，推荐）

- Boxie 无臂 → **手渡し不可**。解法：机器人旁置**出签口（くじ dispenser）**，
  机器人用头部朝向＋言语引导取签。
- **成功判定＝出签口传感器**（断光/微动开关）：签被抽走 → SUCCESS 自动记录。
  与 P1-2 的"消毒液喷头触发"完全同型——这是范式复刻里最有价值的一项：
  **成果事件的仪器化无需新方法**。
- child 型 humanoid（有手）为备选：手渡し更自然但需挂载确认＋工期风险；
  Boxie＋dispenser 为 9 月工期下的安全解。

## 7. 指标（全部自动记录；印象量表不作主证据——P5 教训）

**分母设计的关键原则（易踩坑）**：分母必须**外生**（条件无关）。
"进入半径 r 且朝向机器人"是**机器人行为的结果**——C2 若更早招呼会制造更多"接近者"，
分母被条件污染，配布率反而可能被拉低。→ **统计分母＝通過者数**（过线人数，由人流决定）；
"半径 r"只作状态机触发条件（行为层），与统计定义分家。

**漏斗结构**：

```
通過者（外生分母）
  └→ 立ち止まり     ← 誘引率：C2 优势假说作用点①（先转向→HELLO 及时）
       └→ ENGAGED（KUJI 劝诱到达）
            └→ SUCCESS（取签）＝主指标分子
```

| 层 | 指标 | 定义 |
|---|---|---|
| **主** | **配布率** | SUCCESS 数 ÷ **通過者数**（块内） |
| 副 | 誘引率 | 立ち止まり数 ÷ 通過者数 |
| 副 | 立ち止まり→取签转化 | SUCCESS ÷ 立ち止まり数 |
| 副 | 応対の速さ | 接近/发声 → TRACK 转向的时延（C2 应更早乃至为负） |
| 副 | 取りこぼし | 多人场面中未被 HELLO 而离开的人数（作用点②） |
| 检查 | 先読み性 manipulation check | tick 级注意日志 → P5 行为监查移植：确认 C2 确实"先转" |
| 协变量 | teleop 介入次数、人流量、时间带 | 混合模型协变量 |

**归因链（每环有日志）**：先转向（応対の速さ）→ HELLO 及时（誘引率）→
KUJI 到达（ENGAGED）→ 取签（配布率）。有差时可定位在哪一环产生；无差时可报告链条哪环就断。

### 7.1 通過者数的计测系

1. **专用固定摄像头**（三脚架/壁挂广角俯拍通道）——**不可用机器人自身相机**：
   机器人相机随头转动，头的朝向正是实验变量，用它计数＝实验变量二次污染分母。
2. **检测＋跟踪＋虚拟过线计数**（YOLO＋ByteTrack 类；word-wolf-exp-eval utils 的
   人物检测代码可复用）。track ID 过线一次计一次；群体按个人计数；
   虚拟线全条件全时段固定同一条。
3. **人工校准闭环**（预备实验）：抽样时段人工点数 vs 自动计数，误检率目标 <5%；
   同时定规则：工作人员/回头客的排除标记、儿童的处理（兼顾伦理）。
4. **混乱度分层**用同一摄像头的同时 track 数判定（1 人 vs 多人），不新增设备。

## 8. 对结果正负的两面准备（博论头部准则）

- **C2 > C1**：先読み注意在帰結成立时产生任务价值 → 矩阵右下格闭合为"有"。
- **无差**：结合 P5 → "此类轻交互中注意时机不是瓶颈" → 闭合为"无"，设计含意同样明确
  （资源应投向运动质量/存在感）。
- **不依赖方向的保底贡献**：P1-2 范式上的统合系统现场运转实证＋行为监查的现场移植＋
  配布率指标的确立＋（若 A 案实现）IRL 行动策略在新任务的复用。

## 9. 时间线与 TODO

| 时期 | 内容 |
|---|---|
| 〜8/2 | （Item 3）新数据 → 新 PSSP 模型（本实验感知引擎） |
| 8 月上-中 | 设计定稿・出签口制作・场地/伦理手续・r と t の预校准 |
| 8 月下 | 预备实验（1 日）：分母校准、状态机参数、（可选）teleop 示范采集 → A 案判断 |
| 9 月上 | 本实验（周末 2 日） |
| 9/14 | 中間発表：博论构成＋（尽可能）初期结果 |

- [ ] 场地确保与许可（理研内？商业设施？）——人流量决定 C3 取舍
- [ ] くじ内容与伦理：奖品价值、拍摄掲示、未成年应对
- [ ] 通過者数（分母）计测系的实装（固定摄像头＋过线计数，§7.1）与预备实验校准
      ——**主指标成立的前提，最优先**；含固定摄像头的架设位置确认（随场地定）
- [ ] 出签口传感器的制作与误检率
- [ ] PSSP domain shift：word wolf 会话 → 街头出入り；新数据（Item 3）覆盖度确认，
      必要时预备实验补采微调数据
- [ ] 行动层 B 案（状态机）实装；A 案（IRL）可行性在预备实验后判断
- [ ] Boxie 续用确认（PSSP 传感模块＋dispenser 联动）
