# PC-D（高性能サーバ）固有の設定
#
# PC-D は理研にあり、PC-A/B/C の LAN には居ない。PC-C 側にも理研側にも
# 着信ポートが無いので、**PC-C から張った SSH のポート転送**を通って
# OME に繋ぐ（詳細は todo-list.md「PC-D だけ別の場所にある」）。
#
#   PC-C 側:  ./run.sh   (PCD_SSH_HOST を設定しておくとトンネルも張る)
#   その結果、PC-D の localhost に OME の 3333 と 3478 が生える。
#
# 同じ LAN に移した場合は、この 2 行を消せば common/config.env の
# 既定（PC_C_IP へ直接・UDP）に戻る。
export OME_HOST=127.0.0.1
export OME_USE_TURN=1     # SSH は TCP しか運べないので TURN/TCP を使う

# PC-D の ROS は **galactic**（PC-B / PC-C は humble）。決め打ちで上書きする。
#
# なお PC-D で ROS が要るのは head_controller.py（頭部指令の publish）だけで、
# OME からの受信（recv_ome.py / ome_receiver.py）は rclpy を使わない。
# distro 差が問題になるのは PC-B との通信だけ。詳細は todo-list.md。
export ROS_DISTRO_SETUP=/opt/ros/galactic/setup.bash
