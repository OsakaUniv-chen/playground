__pycache__/
*.pyc
.DS_Store

# model checkpoints — never commit, regardless of path (see CLAUDE.md: no files >10MB)
*.pt
*.pth
*.ckpt
*.safetensors
*.onnx

# generated QC videos
video-generator/results/**/*.mp4

# PSSP: preprocessing-generated training data (npz), never commit -- large,
# regeneratable from the raw rosbags. Matches train-pssp/first-run/data/,
# train-pssp/second-run/data/, etc. (see train-pssp/CONTEXT.md run-folder convention).
train-pssp/*-run/data/
